const readline = require('readline-sync');
// questionFloat para numeros quebrados

let numero = Number(readline.question("Digite um numero para a soma "))
let segNumero = Number(readline.question("Digite um outro numero para a soma "))

console.log(numero+segNumero)
